{
    "name": "portfolio-admin-system",
    "private": true,
    "scripts": {
      "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix admin\"",
      "backend": "npm run dev --prefix backend",
      "admin": "npm run dev --prefix admin",
      "install-all": "npm install --prefix backend && npm install --prefix admin"
    },
    "devDependencies": {
      "concurrently": "^9.2.0"
    }
  }